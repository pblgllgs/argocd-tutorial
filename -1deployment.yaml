apiVersion: apps/v1
kind: Deployment
metadata:
  name:  nginx
  namespace: prod
  labels:
    name:  nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        name:  nginx
    spec:
      containers:
        - image:  nginx:1.25.2
          name:  nginx
          ports:
            - containerPort: 80
